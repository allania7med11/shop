name: Build & push images

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          # If shop_back / shop_front are PRIVATE, the default token can't read them —
          # set a PAT secret with repo access and uncomment:
          # token: ${{ secrets.SUBMODULE_PAT }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Write env files (from secrets)
        # shop_front/.env is baked into the front bundle by the nginx image build.
        # shop_back/.env is only needed so compose can parse env_file at build time;
        # it is dockerignored and never copied into any image.
        run: |
          cat > shop_back/.env <<'EOF'
          ${{ secrets.BACKEND_ENV }}
          EOF
          cat > shop_front/.env <<'EOF'
          ${{ secrets.FRONTEND_ENV }}
          EOF

      - name: Build & push prod images
        # The nginx image builds front (export) + back (collectstatic) internally
        # via multi-stage — no host bind mounts, so no CI uid permission issues.
        run: |
          docker compose -f docker-compose.prod.yml build
          docker compose -f docker-compose.prod.yml push
